Create Ore Expansion 更新日志
日期：2026-08-21

一、新增机器：翡翠应力充能器（jade_create_charger）
1. 六向应力机器（可水平/竖直放置，优先对准相邻传动轴）
   - 按转速分三档充能态：低（≤½ 上限转速）/ 高（½~上限转速）/ 伽马（≥上限转速）
   - 上限转速读取 Create 配置 maxRotationSpeed（默认 256 RPM，可调，不硬编码）
   - 蓄满自动发射对应等级能量波（音效：音符盒钟声），发射头带蓄力收缩/弹出动画
2. 能量波命中效果：
   - 生物：造成伤害（低 4 / 高 6 / 伽马 10）
   - 掉落物/置物台物品：能量工具充能（100/500/1000 点）或按配方转化（命中音效：紫水晶共鸣）
   - 粒子用原版染色粒子（DustParticleOptions），零自定义粒子注册、零卡顿
3. 护目镜提示：充能态 + 蓄力进度能量条（复用 BarTooltipRender，颜色随等级）

二、充能配方体系统一（关键架构改进，方便第三方适配）
1. 配方类型由三个（charging / charging_high / charging_gamma）合并为**一个** createoreexpansion:charging
2. 等级改为配方自带字段 level（1=低、2=高、3=伽马）：
   { "type": "createoreexpansion:charging", "level": 2, "ingredients": [...], "results": [...] }
3. 机器按等级匹配：能量波等级 ≥ 配方 level 即加工（取等级最高者），等级不够跳过
4. 第三方/整合包适配只需写一条带 level 字段的配方 JSON，无需任何 Java 代码

三、JEI 展示
1. 低/高/伽马三个分类合并为一个「充能加工」分类，配方卡片上以等级徽章区分（低=黄、高=绿、伽马=蓝）
2. 分类图标去掉右下角翡翠锭叠加（仅充能器图标）
3. 传动轴改为 4px 短轴（替代 Create 全尺寸 16px 轴）防止穿模
   - 修正位置：blockstate 旋转绕方块中心，接口落在 y=16，轴模型定位于 y 12~16（机身下方）
4. 工具充能配方（31 物品 × 3 等级 = 93 条）统一生成到 tool_charge/ 一个文件夹，
   等级由 level 字段区分，文件名后缀 _low/_high/_gamma 仅保证 id 唯一

四、光照规则修复（仿 Create 机器）
1. 动力角磨床、翡翠应力充能器不再阻挡光照：
   - 天空光可无衰减穿透（propagatesSkylightDown = true）
   - 自身面不做环境光遮蔽变暗（getShadeBrightness = 1.0）
   - 修复：坑里放创造马达、上方放充能器时马达亮度骤降的问题
2. 角磨床盖板/开盖伸出部分发黑修复：getShape 改为非全方块（AO 不再自遮蔽）+
   模型加 ambientocclusion:false（面统一用方块格光照，不再采样伸出位置的相邻格）

五、动力角磨床完善
1. 加工物品渲染（仿置物台，参照 Create Depot）：未加工物品优先显示；
   只有成品时显示成品；堆叠效果（1 个/多个/一组）与置物台一致；
   普通物品渲染 y 上移修复（原被底座 Base 遮挡不显示）
2. 输出逻辑统一：成品一律存入库存（槽 1+）——
   - 有漏斗（侧面/底面）：漏斗随时抽取
   - 无漏斗：空手右键第一次取成品（全部）、第二次取原料（误放的物品）
   - 开盖空手右键也能取物品（无物品时取角磨轮）
3. 修复物品丢失：顶面丢入/开盖放入时，槽满物品保留不消失（成功才 discard/shrink）
4. 粒子飞溅细腻化：碎屑沿角磨轮切向飞出（2~3 个/次），方向跟随轮子旋转
   （getSpeed 正负），速度随转速增强
5. 无匹配配方：不再瞬间消失，按正常加工节奏（有粒子、有耗时）逐个消耗、无产出
6. 高级角磨动态配方：新增 GrinderRecipeTypes 注册表——
   1 级轮=角磨配方，2 级轮+=Create 粉碎轮/石磨配方，3 级轮+=拆磨配方；
   第三方可注册自己的配方类型让角磨床执行（开闭原则）
7. 角磨轮参数配置化：等级最低转速/加工耗时抽到 createoreexpansion-common.toml
   的 grinding 段（tier1/2/3 MinRpm/Time），耗时插值上限跟随 Create maxRotationSpeed

六、工具充能配方重构
1. 新增 ChargingRecipeTools 注册器（单一数据源）：能量工具在 AllItems 注册处
   自动挂接（SkillItemBuilder 的 onRegister），凝能佩注册后一行声明；
   配方生成器从注册器收集物品，不再另维护 31 项列表
2. 修复 18→93 条：挂接从 SkillItemBuilder.register()（链末尾实际是 ItemBuilder.register()）
   移到 build() 的 onRegister 回调
3. 配方生成分工：拆磨（61 条：原版 4 组装备 + 模组 5 组工具）与工具充能（93 条）
   由生成器批量生成；粗矿序列加工配方（4 条）手写 JSON（Create sequenced_assembly 格式）

七、充能掉落物显示修复
1. 充能后 Jade 等外部 mod 显示旧能量：ItemEntity.setItem 传副本（新引用）触发数据刷新
   （原地修改 + setItem(同引用) 不触发更新），置物台同理（insertItem 传副本）

八、代码整理（OOP：对外扩展开放、对内修改封闭）
1. GrindingWheelTier 移入 content/grinding/item/ 包（与 GrindingWheelItem 统一角磨轮领域）
2. 充能波实体 getSpeedBlocks/getDamage 改 protected（子类如雷鸣波可定制速度/伤害）
3. GrinderRecipeTypes 注册表（配方类型按等级挂接，本模组代码不感知具体类型）
4. 充能器抽象基类（AbstractCreateChargerBlock/BlockEntity/WaveEntity），雷鸣充能器继承即可
